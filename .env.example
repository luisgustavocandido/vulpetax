# Banco de dados (PostgreSQL) — local via Docker (intranet)
# Subir: docker compose up -d
# Aplicar migrações: npm run db:migrate
# Exemplo (ajuste user/password para bater com POSTGRES_* do docker-compose):
DATABASE_URL=postgresql://user:password@localhost:5432/vulpetax

# Auth por passcode (MVP interno)
# DISABLE_AUTH=true — desativa login temporariamente (só para dev; não usar em produção)
# PASSCODE ou PASSCODE_CURRENT: segredo para assinar/validar sessão.
# PASSCODE_PREVIOUS (opcional): permite rotação sem derrubar sessões ativas.
PASSCODE=defina-um-passcode-seguro-aqui
# PASSCODE_CURRENT=passcode-atual
# PASSCODE_PREVIOUS=passcode-anterior

# Sync TAX Form via Google Sheets
# GOOGLE_SERVICE_ACCOUNT_EMAIL=xxx@projeto.iam.gserviceaccount.com
# GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# GOOGLE_SHEETS_SPREADSHEET_ID=1fE61l3c8aDGMOUtlMx9RNIlhs-vpJgctI6VMwOEqx3s
# GOOGLE_SHEETS_GID=129689283
# GOOGLE_SHEETS_SHEET_NAME=TaxForm2026
# CRON_SECRET=seu-secret-para-o-endpoint-de-sync

# PDF Pós-Venda LLC — conversão DOCX→PDF via HTTP
# PDF_CONVERTER_PROVIDER=gotenberg|cloudconvert (padrão: gotenberg)
# GOTENBERG_URL=http://localhost:3001
# GOTENBERG_URL=http://gotenberg:3000
# PDF_CONVERSION_TIMEOUT_MS=60000
# PDF_CONVERSION_RETRY_COUNT=2
# CLOUDCONVERT_API_KEY=...
# CLOUDCONVERT_SANDBOX=false|true

# Sync Pós-Venda LLC via Google Sheets (mesmo Service Account + CRON_SECRET)
# POSVENDA_SHEETS_SPREADSHEET_ID=1Ei9G-lhTmAVOk7ItJy89A_HohhUpWhgz8kZfG_xDc6w
# POSVENDA_SHEETS_GID=1004562294
# POSVENDA_SHEETS_SHEET_NAME=NomeDaAba
